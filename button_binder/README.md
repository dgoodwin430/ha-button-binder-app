# Button Binder

Button Binder learns button events and maps them to Home Assistant service calls.

Open the app web UI, add a button interface, press **Learn** on a binding, press the physical button, then choose the Home Assistant action to run.

See `DOCS.md` for setup details.
